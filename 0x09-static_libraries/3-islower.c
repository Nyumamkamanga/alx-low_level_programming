#include "main.h"

/**
 * _islower - Checks for lowercase character
 * @c: The character is to check
 * Return: 1 if c lowercase, 0 otherwise
 */

int _islower(int c)

{
	return (c >= 'a' && c <= 'z');
}
